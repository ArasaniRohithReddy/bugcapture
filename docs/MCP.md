# MCP

The optional `mcp/` package provides a stdio MCP-compatible server for exported JSON reports. Set `BUGCAPTURE_REPORTS` to an export folder, then run `npm install` and `npm run build` in `mcp/`. Tools list and search reports without network access.
