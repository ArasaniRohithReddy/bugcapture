# Integrations

BugCapture has no hosted backend, OAuth flow, provider key, or automatic third-party integration. Use JSON, Markdown, ZIP, or the local-only AI prompt copy action to move a report into a workflow of your choice.
